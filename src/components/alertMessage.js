const getAlertMessage = () => {
    const alertMessage = "ChatGPT sometimes writes plausible-sounding but incorrect answers. Does this information seem accurate?"
    return alertMessage;
}

export default getAlertMessage;